export enum RouterPath {
  Main = '/',
  Cart = '/cart',
  Products = '/products',
}

export enum UrlParamKey {
  Page = 'page',
  Limit = 'limit',
  Category = 'category',
  Brand = 'brand',
  Price = 'price',
  Stock = 'stock',
}

export enum PromoCodes {
  RS = 'RS',
  EMP = 'EMP',
}