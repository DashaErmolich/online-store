export enum RouterPath {
  Main = '/',
  Cart = '/cart',
  Products = '/products/:id',
}

export enum UrlParamKey {
  Page = 'page',
  Limit = 'limit',
  Category = 'category',
  Brand = 'brand',
  Price = 'price',
  Stock = 'stock',
}